## API Report File for "@pago/reactive"

> Do not edit this file. It is a report generated by [API Extractor](https://api-extractor.com/).

```ts

import { Context } from 'react';
import { ReactElement } from 'react';

// @public
export const createElement: (type: any, ...rest: any[]) => JSX.Element;

// @public
export function derived<T>(fn: () => T): ReadonlyRef<T>;

// @public
export interface Effect {
    // (undocumented)
    (): void;
}

// Warning: (ae-forgotten-export) The symbol "Effect" needs to be exported by the entry point index.d.ts
//
// @public
export function effect(fn: (onInvalidate: (teardown: Effect_2) => void) => void): void;

// @public
export function fromHook<T>(fn: () => T): Ref<T>;

// @public
export function inject<T>(context: Context<T>): ReadonlyRef<T>;

// @public
export const jsx: (type: any, ...rest: any[]) => JSX.Element;

// @public
export const jsxs: (type: any, ...rest: any[]) => JSX.Element;

// @public
export function memoize<T>(fn: () => T, controller?: SubscriptionController): () => T;

// @public
export function r(render: () => JSX.Element): JSX.Element;

// @public
export function reactive<T extends object>(initialValue: T): Store<T>;

// @public
export function readonly<T>(ref: Ref<T>): ReadonlyRef<T>;

// @public
export interface ReadonlyRef<T> {
    // (undocumented)
    readonly current: T;
}

// @public
export interface Ref<T> extends ReadonlyRef<T> {
    // (undocumented)
    current: T;
}

// @public
export function ref<T>(initialValue: T): RefObject<T>;

// @public
export type RefContainer<T> = {
    readonly [P in keyof T]: RefObject<T[P]>;
};

// @public
export interface RefObject<T> extends Ref<T> {
    // (undocumented)
    update(fn: (value: T) => T): void;
}

// @public
export type Store<T> = {
    [P in keyof T]: T[P] extends Ref<any> ? T[P]['current'] : T[P];
};

// @public
export class SubscriptionController {
    constructor(effect: Effect);
    cleanup?: Effect;
    effect: Effect;
    // Warning: (ae-forgotten-export) The symbol "Tag" needs to be exported by the entry point index.d.ts
    // Warning: (ae-forgotten-export) The symbol "Revision" needs to be exported by the entry point index.d.ts
    //
    // @internal (undocumented)
    setObservedTags(tags: Array<Tag>, lastRevision: Revision): void;
    subscribe(): void;
    unsubscribe(): void;
    }

// @public
export function toRef<T extends object, K extends keyof T>(store: T, prop: K): RefObject<T[K]>;

// @public
export function toRefs<T extends {
    [key: string]: any;
}>(store: Store<T>): RefContainer<T>;

// @public
export function useRefValue<T>(ref: RefObject<T>): T;

// @public
export function watchEffect(fn: (onInvalidate: (teardown: Effect) => void) => void): Effect;

// Warning: (ae-forgotten-export) The symbol "RenderFunction" needs to be exported by the entry point index.d.ts
// Warning: (ae-forgotten-export) The symbol "RenderResult" needs to be exported by the entry point index.d.ts
//
// @public
export function wrap<T extends object>(construct: (props: T) => RenderFunction | RenderResult): {
    (props: T): RenderResult;
    displayName: any;
};


// (No @packageDocumentation comment for this package)

```
